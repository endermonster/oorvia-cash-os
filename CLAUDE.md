@AGENTS.md

## Git Workflow

When the user says "commit", "commit changes", "commit this", or any similar phrasing:
1. Stage the relevant changed files
2. Commit with an appropriate message
3. **Immediately push to GitHub (`git push`)** — do not wait to be asked

Always commit + push as a single action unless the user explicitly says "commit but don't push".
